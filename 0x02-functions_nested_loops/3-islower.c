#include "main.h"
/**
 * _islower - checking for lowercase
 * @c: interger value
 *Return: lower if true, upper if false
 */
int _islower(int c)
{
	int x = 'a';

	for (x = 'a'; x <= 'z'; x++)
	{
		if (c == x)
		{
			return (lower);
		}
	}
	return (upper);
}
